<h1><%= @keyword.title %></h1>

<% if @keyword.publications? %>
  <ul>
  <% @keyword.publications.each do |publication| %>
    <li><%= link_to publication.title, publication_title_path(publication.url_friendly) %></li>
  <% end %>
  </ul>
<% end %>