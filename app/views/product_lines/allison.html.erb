<div id="product-line" class="allison">
  <h1><%= @product_line.name %></h1>
  <div id="col-1">
    <div class="description">
      <%= @product_line.description %>
    </div>
    
    <ul>
      <li><%= link_to 'Part and Application Finder', product_line_parts_path(@product_line.url_friendly) %></li>
      <li><%= link_to 'New Parts', recent_product_line_parts_path(@product_line.url_friendly) %></li>
      <li><%= link_to 'Distributors', distributors_path %></li>
      <li><%= link_to 'Email Sign Up', subscribe_to_email_newsletter_path, :class => 'email' %></li>
      <li><%= link_to 'Contact Us', new_contact_path %></li>
    </ul>
  </div>
  
  <div id="col-2">
    <% if @product_line.featured_parts.any? %>
    <h2>Featured Parts</h2>
      <% @product_line.featured_parts.each do |part| %>
      <div class="featured">
        <% if part.part.primary_photo? %>
          <p><%= image_tag part.part.primary_photo.photo.asset.url(:thumbnail) %></p>
        <% end %>
        <p><strong>Featured Product:</strong> <%= link_to part.part.name, product_line_part_path(@product_line.url_friendly, part.part.url_friendly) %></p>
        <p><strong>Make:</strong> <%= link_to 'General Motors', '#' %></p>
        <p><strong>Unit:</strong> </p>
        <p class="description"><%= part.part.description %></p>
      </div>
      <% end %>
    <% end %>
  </div>
</div>
