<h1><%= @author.full_name %></h1>

<% if @author.publications? %>
  <ul>
  <% @author.publications.each do |publication| %>
    <li><%= link_to publication.title, publication_title_path(publication.url_friendly) %></li>
  <% end %>
  </ul>
<% end %>